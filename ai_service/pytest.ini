[pytest]
testpaths = tests
asyncio_mode = auto
# Every test function gets a fresh event loop, so a singleton that caches an
# httpx/Redis client bound to one loop can't leak into the next test.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
